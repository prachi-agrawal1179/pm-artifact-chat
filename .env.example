# Optional. Without this key, retrieval still works and answers are extractive quotes.
OPENAI_API_KEY=

# gpt-4o-mini is cheap and good enough for grounded Q&A
OPENAI_MODEL=gpt-4o-mini

# huggingface = semantic + keyword hybrid retrieval, fully local
EMBEDDING_PROVIDER=huggingface
